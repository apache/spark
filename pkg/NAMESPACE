#exportPattern("^[[:alpha:]]+")
exportClasses("RRDD")
exportMethods(
              "cache",
              "collect",
              "count",
              "length",
              "lapply",
              "lapplyPartition",
              "reduce"
             )

# S3 methods exported
export("textFile", "parallelize")
exportPattern("^sparkR")
