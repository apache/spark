#exportPattern("^[[:alpha:]]+")
exportClasses("RRDD")
exportMethods(
              "cache",
              "collect",
              "count",
              "length",
              "lapply",
              "lapplyPartition",
              "reduce",
              "take",
              "partitionBy"
             )

# S3 methods exported
export("textFile", "parallelize")
exportPattern("^sparkR")
