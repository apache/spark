#exportPattern("^[[:alpha:]]+")
exportClasses("RRDD")
exportMethods(
              "cache",
              "collect",
              "collectPartition",
              "count",
              "length",
              "lapply",
              "lapplyPartition",
              "partitionBy",
              "reduce",
              "take"
             )

# S3 methods exported
export("textFile", "parallelize")
exportPattern("^sparkR")
