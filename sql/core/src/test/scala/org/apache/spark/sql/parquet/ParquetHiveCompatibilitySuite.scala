package org.apache.spark.sql.parquet

class ParquetHiveCompatibilitySuite extends ParquetCompatibilityTest {
  override protected def beforeAll(): Unit = {
    super.beforeAll()
  }

  test("Read Parquet files genenrated by parquet-hive") {

  }
}
