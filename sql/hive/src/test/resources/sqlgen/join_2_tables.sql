-- This file is automatically generated by LogicalPlanToSQLSuite.
SELECT COUNT(a.value), b.KEY, a.KEY
FROM parquet_t1 a, parquet_t1 b
GROUP BY a.KEY, b.KEY
HAVING MAX(a.KEY) > 0
--------------------------------------------------------------------------------
SELECT `count(value)`, b.`KEY`, a.`KEY` FROM (SELECT count(a.`value`) AS `count(value)`, b.`KEY`, a.`KEY`, max(a.`KEY`) AS `max(KEY)` FROM (parquet_t1) AS a INNER JOIN (parquet_t1) AS b GROUP BY a.`KEY`, b.`KEY` HAVING (`max(KEY)` > CAST(0 AS BIGINT)))
