-- This file is automatically generated by LogicalPlanToSQLSuite.
select key, count(*)
from src
group by key
having count(*) in (select count(*) from src s1 where s1.key = '90' group by s1.key)
order by key
--------------------------------------------------------------------------------
SELECT src.`key`, `count(1)` FROM (SELECT src.`key`, count(1) AS `count(1)` FROM `default`.`src` GROUP BY src.`key` HAVING `count(1)` IN (SELECT count(1) AS `count(1)` FROM (`default`.`src`) AS s1 WHERE (CAST(s1.`key` AS DOUBLE) = CAST('90' AS DOUBLE)) GROUP BY s1.`key`)) ORDER BY src.`key` ASC
