# Gradle JVM configuration for Spark build
org.gradle.jvmargs=-Xmx12g -XX:MaxMetaspaceSize=4g -XX:+UseG1GC

# Gradle daemon configuration
org.gradle.daemon=true
org.gradle.parallel=false
org.gradle.configureondemand=false

# Increase heap size for Scala compilation tasks
org.gradle.workers.max=2